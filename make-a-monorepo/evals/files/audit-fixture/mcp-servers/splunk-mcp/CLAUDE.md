# MCP Server: splunk-mcp

## Purpose
Wraps the Splunk REST API for search and alerting.

## Tools exposed
- `search_events` — run an SPL search and return matching events

## Env vars
- `SPLUNK_TOKEN` — API auth token
- `SPLUNK_HOST` — Splunk instance base URL

## Local conventions
None beyond root CLAUDE.md.
