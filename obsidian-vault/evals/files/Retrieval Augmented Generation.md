# Retrieval Augmented Generation

RAG combines a retriever over an external knowledge base with a generative language model, so the model conditions its output on documents fetched at query time instead of relying only on parameters learned during training.

## Related

- [[RAG Index]]
